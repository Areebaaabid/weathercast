{
  "name": "weather-mern",
  "private": true,
  "scripts": {
    "build": "cd client && npm run build",
    "dev": "cd client && npm run dev",
    "postinstall": "cd client && npm install"
  },
  "dependencies": {
    "pg": "8.11.3"
  }
}
